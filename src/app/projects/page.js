import ProjectsSection from '@/components/ProjectsSection'; // Adjust the import path as needed

const ProjectsPage = () => {
  return (
    <div>
      <ProjectsSection />
    </div>
  );
};

export default ProjectsPage;